import React from 'react'

const Box = ({num, random}) => {
    for (let i=0; i<5; i++) {
        if (num === random[i]) {
            
        }
    }
  return (
    <div className={'box '}>

    </div>
  )
}

export default Box;